import { createServer, request as httpRequest } from 'node:http';
import { spawn } from 'node:child_process';

const port = Number(process.env.PORT || 10000);
const upstreamPort = port + 1;
const views = [];
const now = new Date();
for (let i = 1; i <= 6; i++) { const d = new Date(now); d.setDate(d.getDate() - i); for (let n = 0; n < 3 + (i % 3); n++) views.push(d.toISOString()); }
for (let i = 1; i <= 11; i++) { const d = new Date(now.getFullYear(), now.getMonth() - i, 12); for (let n = 0; n < 8 + (i % 5); n++) views.push(d.toISOString()); }
for (let i = 1; i <= 4; i++) { const d = new Date(now.getFullYear() - i, 6, 10); for (let n = 0; n < 20 + i * 4; n++) views.push(d.toISOString()); }

spawn(process.execPath, ['home-nav-proxy.mjs'], { cwd: new URL('.', import.meta.url), env: { ...process.env, PORT: String(upstreamPort) }, stdio: 'inherit' });
const sameDay = (a,b) => a.getFullYear()===b.getFullYear() && a.getMonth()===b.getMonth() && a.getDate()===b.getDate();
const sameMonth = (a,b) => a.getFullYear()===b.getFullYear() && a.getMonth()===b.getMonth();
const sameYear = (a,b) => a.getFullYear()===b.getFullYear();
function analytics(){
  const current=new Date(), daily=new Map(), monthly=new Map(), yearly=new Map();
  for(const value of views){const d=new Date(value);const day=d.toISOString().slice(0,10);const month=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`;const year=String(d.getFullYear());daily.set(day,(daily.get(day)||0)+1);monthly.set(month,(monthly.get(month)||0)+1);yearly.set(year,(yearly.get(year)||0)+1);}
  return {total:views.length,today:views.filter(v=>sameDay(new Date(v),current)).length,month:views.filter(v=>sameMonth(new Date(v),current)).length,year:views.filter(v=>sameYear(new Date(v),current)).length,daily:[...daily.entries()].sort().slice(-14).map(([date,count])=>({date,count})),monthly:[...monthly.entries()].sort().slice(-12).map(([month,count])=>({month,count})),yearly:[...yearly.entries()].sort().slice(-6).map(([year,count])=>({year,count}))};
}

const ANALYTICS_UI = `<style id="seller-view-analytics-style">
.view-analytics{margin-top:18px;border:1px solid #35333a;background:linear-gradient(145deg,#15161a,#0d0e11);padding:20px 22px}.view-analytics-head{display:flex;justify-content:space-between;align-items:center}.view-analytics-head strong{font-size:16px}.view-analytics-head span{font-size:8px;letter-spacing:.15em;color:#c6a864}.view-analytics-metrics{display:grid;grid-template-columns:repeat(4,1fr);margin-top:13px;border-top:1px solid #2b2a30;border-bottom:1px solid #2b2a30}.view-analytics-metric{padding:11px 15px;border-right:1px solid #2b2a30}.view-analytics-metric:last-child{border-right:0}.view-analytics-metric b{display:block;font-size:7px;letter-spacing:.13em;color:#c6a864}.view-analytics-metric strong{display:inline-block;font-size:22px;margin-top:3px}.view-analytics-metric span{margin-left:5px;color:#77736e;font-size:8px}.view-analytics-toolbar{display:flex;justify-content:space-between;align-items:center;margin-top:15px}.view-analytics-period-title{font-size:10px;font-weight:600;color:#e8e3da}.view-analytics-period-title small{font-weight:400;color:#77736e;margin-left:6px}.view-analytics-periods{display:flex;border:1px solid #343239}.view-analytics-periods button{border:0;border-right:1px solid #343239;background:#111216;color:#918d88;padding:6px 12px;font-size:8px;cursor:pointer}.view-analytics-periods button:last-child{border-right:0}.view-analytics-periods button.active{background:#c6a864;color:#111}.view-analytics-chart{height:118px;margin-top:8px;padding:10px 5px 0;border-top:1px solid #29282d;display:flex;align-items:flex-end;gap:8px;overflow:hidden}.view-analytics-point{height:100%;min-width:30px;flex:1;display:flex;flex-direction:column;justify-content:flex-end;align-items:center}.view-analytics-point b{font-size:8px;color:#f4f1eb;font-weight:600;height:13px}.view-analytics-bar{width:100%;max-width:30px;min-height:4px;background:#c6a864;border-radius:1px 1px 0 0}.view-analytics-point span{font-size:7px;color:#77736e;margin-top:6px;white-space:nowrap}.view-analytics-note{margin-top:8px;color:#6f6b66;font-size:8px}.view-analytics-empty{padding:18px;color:#77736e;font-size:9px;text-align:center}@media(max-width:900px){.view-analytics-metrics{grid-template-columns:repeat(2,1fr)}.view-analytics-metric:nth-child(2){border-right:0}.view-analytics-toolbar{gap:10px}}@media(max-width:600px){.view-analytics{padding:17px}.view-analytics-head span{display:none}.view-analytics-metric{padding:10px}.view-analytics-metric strong{font-size:19px}.view-analytics-toolbar{align-items:flex-start;flex-direction:column}.view-analytics-chart{gap:4px}}
</style>
<section class="view-analytics" id="viewAnalytics"><div class="view-analytics-head"><strong>動画詳細ページ閲覧数</strong><span>PAGE VIEWS / SELLER ANALYTICS</span></div><div class="view-analytics-metrics"><article class="view-analytics-metric"><b>TODAY</b><strong id="viewToday">0</strong><span>今日</span></article><article class="view-analytics-metric"><b>THIS MONTH</b><strong id="viewMonth">0</strong><span>今月</span></article><article class="view-analytics-metric"><b>THIS YEAR</b><strong id="viewYear">0</strong><span>今年</span></article><article class="view-analytics-metric"><b>ALL TIME</b><strong id="viewTotal">0</strong><span>累計</span></article></div><div class="view-analytics-toolbar"><div class="view-analytics-period-title" id="viewAnalyticsTitle">日別の閲覧数<small>直近14日</small></div><div class="view-analytics-periods"><button type="button" class="active" data-period="daily" onclick="window.__sellerViewPeriod('daily')">日別</button><button type="button" data-period="monthly" onclick="window.__sellerViewPeriod('monthly')">月別</button><button type="button" data-period="yearly" onclick="window.__sellerViewPeriod('yearly')">年別</button></div></div><div class="view-analytics-chart" id="viewAnalyticsList"><div class="view-analytics-empty">閲覧データを読み込んでいます。</div></div><div class="view-analytics-note">動画詳細ページの表示回数のみを集計しています。</div></section>
<script id="seller-view-analytics-script">(()=>{let data=null;const labels={daily:['日別の閲覧数','直近14日'],monthly:['月別の閲覧数','直近12か月'],yearly:['年別の閲覧数','直近6年']};window.__sellerViewPeriod=period=>{if(!data)return;document.querySelectorAll('#viewAnalytics [data-period]').forEach(x=>x.classList.toggle('active',x.dataset.period===period));const meta=labels[period]||labels.daily;document.getElementById('viewAnalyticsTitle').innerHTML=meta[0]+'<small>'+meta[1]+'</small>';const rows=(data[period]||[]).slice().reverse();const list=document.getElementById('viewAnalyticsList');if(!rows.length){list.innerHTML='<div class="view-analytics-empty">この期間の閲覧データはありません。</div>';return}const max=Math.max(...rows.map(x=>Number(x.count)),1);list.innerHTML=rows.map(x=>{const label=period==='daily'?x.date.slice(5):period==='monthly'?x.month:x.year;const count=Number(x.count);return '<div class="view-analytics-point"><b>'+count.toLocaleString()+'</b><i class="view-analytics-bar" style="height:'+Math.max(5,Math.round(count/max*76))+'px"></i><span>'+label+'</span></div>'}).join('')};const load=async()=>{try{const r=await fetch('/api/demo/view-analytics',{cache:'no-store'});if(!r.ok)return;data=await r.json();document.getElementById('viewToday').textContent=data.today.toLocaleString();document.getElementById('viewMonth').textContent=data.month.toLocaleString();document.getElementById('viewYear').textContent=data.year.toLocaleString();document.getElementById('viewTotal').textContent=data.total.toLocaleString();window.__sellerViewPeriod('daily')}catch{}};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',load,{once:true});else load()})()</script>`;
function patchCreator(html){if(html.includes('id="viewAnalytics"'))return html;return html.replace('<section class="grid">',`${ANALYTICS_UI}<section class="grid">`)}
const server=createServer((req,res)=>{const pathname=(req.url||'/').split('?')[0];if(req.method==='GET'&&pathname==='/api/demo/view-analytics'){const body=JSON.stringify(analytics());res.writeHead(200,{'content-type':'application/json; charset=utf-8','cache-control':'no-store'});res.end(body);return}const upstream=httpRequest({hostname:'127.0.0.1',port:upstreamPort,path:req.url,method:req.method,headers:req.headers},response=>{const chunks=[];response.on('data',c=>chunks.push(c));response.on('end',()=>{let body=Buffer.concat(chunks);const type=String(response.headers['content-type']||'');if(req.method==='GET'&&pathname==='/pages/product-detail.html'&&response.statusCode===200&&type.includes('text/html')){if(body.toString('utf8').includes('Demo Creator'))views.push(new Date().toISOString())}if(req.method==='GET'&&pathname==='/pages/creator-studio.html'&&response.statusCode===200&&type.includes('text/html'))body=Buffer.from(patchCreator(body.toString('utf8')),'utf8');const headers={...response.headers,'content-length':String(body.length),'cache-control':'no-store, no-cache, must-revalidate, proxy-revalidate',pragma:'no-cache',expires:'0'};delete headers['transfer-encoding'];res.writeHead(response.statusCode||200,headers);res.end(body)})});upstream.on('error',error=>{res.writeHead(502,{'content-type':'text/plain; charset=utf-8'});res.end(`Upstream unavailable: ${error.message}`)});req.pipe(upstream)});
server.listen(port,'0.0.0.0',()=>console.log(`VIDEO MARKETPLACE seller analytics proxy listening on ${port}`));
process.on('SIGTERM',()=>process.exit(0));process.on('SIGINT',()=>process.exit(0));