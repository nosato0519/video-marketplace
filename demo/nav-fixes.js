window.creators=async()=>{const rows=(window.demoState?.products||[]).reduce((m,p)=>(m[p.seller]=(m[p.seller]||[]).concat(p),m),{});openModal(`<div class="ey">CREATORS</div><h2>Creators worth discovering</h2><div class="steps">${Object.entries(rows).map(([name,items])=>`<div class="step"><b>${name} · ★ ${Math.max(...items.map(x=>x.rating)).toFixed(1)}</b><span>${items.length} demo products · verified creator</span></div>`).join('')}</div>`)};
