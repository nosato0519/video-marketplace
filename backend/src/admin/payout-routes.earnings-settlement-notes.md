The payout status transition to `paid` must settle its `payout_earnings_allocations` against `seller_earnings`. Keep this logic in the same transaction as the payout status transition. An earning becomes `paid` only when cumulative allocations from payouts in `paid` status cover its full `net_amount`; partial allocations remain `available`. This checkpoint is intentionally documented before wiring the route so the accounting invariant is explicit.