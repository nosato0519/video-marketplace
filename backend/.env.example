# Runtime
NODE_ENV=development
PORT=3000

# Database (do not commit real credentials)
DATABASE_URL=

# Auth / session secrets (use a strong secret in deployment)
SESSION_SECRET=

# Storage provider configuration will be added when media infrastructure is implemented.
STORAGE_BUCKET=
STORAGE_REGION=

# Payment providers will be configured only after provider/content/country requirements are reviewed.
PAYMENT_PROVIDER=
