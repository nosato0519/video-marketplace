name: CI Trigger

on:
  push:
    branches: [feat/seller-application]

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm latest commit
        run: echo "CI trigger reached for feat/seller-application"
